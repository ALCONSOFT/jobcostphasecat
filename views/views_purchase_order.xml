<odoo>
    <data>
        <!-- 2026-01-30: Restringir picking_type_id a tipos de Recepción (no Devolución) -->
        <record id="purchase_order_form_picking_type_restriction" model="ir.ui.view">
            <field name="name">purchase.order.form.picking.type.restriction</field>
            <field name="model">purchase.order</field>
            <field name="inherit_id" ref="purchase_stock.purchase_order_view_form_inherit"/>
            <field name="priority">100</field>
            <field name="arch" type="xml">
                <!-- Modificar domain para excluir tipos de Devolución (solo mostrar Recepciones) -->
                <xpath expr="//field[@name='picking_type_id']" position="attributes">
                    <attribute name="domain">[('code', '=', 'incoming'), ('return_picking_type_id', '!=', False), '|', ('warehouse_id', '=', False), ('warehouse_id.company_id', '=', company_id)]</attribute>
                </xpath>
            </field>
        </record>

        <!-- Heredar la vista tree de purchase.order sdp-->
        <record id="purchase_order_inherit_purchase_order_tree" model="ir.ui.view">
            <field name="name">purchase.order.inherit.purchase.order.tree</field>
            <field name="model">purchase.order</field>
            <field name="inherit_id" ref="purchase.purchase_order_kpis_tree"/>
            <field name="arch" type="xml">
                <!-- Insertar los campos después del campo 'invoice_status' -->
                <xpath expr="//field[@name='invoice_status']" position="after">
                    <field name="vehicle_id" optional="show"/>
                    <field name="account_analytic_id" optional="show"/>
                    <field name="pr_ref_id" optional="show"/>
                    <field name="payment_term_id" optional="show"/>
                </xpath>
                <xpath expr="//field[@name='user_id']" position="attributes">
                    <attribute name="string">Usuario</attribute>
                </xpath>
                <!-- Agregando colores a los estados agregados
                                        <field name="state" optional="show" widget="badge" decoration-success="state == 'purchase' or state == 'done'" decoration-warning="state == 'to approve'" decoration-info="state == 'draft' or state == 'sent'"/>-->
                <xpath expr="//field[@name='state']" position="replace">
                    <field name="state" optional="show" widget="badge"
                            decoration-success="state == 'purchase' or state == 'done'"
                            decoration-warning="state == 'to approve'"
                            decoration-info="state == 'draft' or state == 'sent'"
                            decoration-danger="state == 'waiting_for_price_revision' or 
                                state == 'waiting_for_price_approval' or
                                state == 'waiting_for_approval' "
                            decoration-it="state == 'waiting_for_price_revision' or 
                                state == 'waiting_for_price_approval'"
                            decoration-bf="state == 'waiting_for_approval'" />
                </xpath>
            </field>
        </record>
        <!-- Heredar la vista tree de purchase.order po-->
        <record id="purchase_order_inherit_purchase_order_tree_jc" model="ir.ui.view">
            <field name="name">purchase.order.inherit.purchase.order.tree</field>
            <field name="model">purchase.order</field>
            <field name="inherit_id" ref="purchase.purchase_order_view_tree"/>
            <field name="arch" type="xml">
                <!-- Insertar los campos después del campo 'invoice_status' -->
                <xpath expr="//field[@name='invoice_status']" position="after">
                    <field name="vehicle_id" optional="show"/>
                    <field name="account_analytic_id" optional="show"/>
                    <field name="payment_term_id" optional="show"/>
                </xpath>
            </field>
        </record>
        <!-- Heredar la vista form de purchase.order.form-->
        <record id="purchase_order_form_custom_fields" model="ir.ui.view">
            <field name="name">purchase.order.form.custom.fields</field>
            <field name="model">purchase.order</field>
            <field name="inherit_id" ref="purchase.purchase_order_form"/>
            <field name="arch" type="xml">
                <xpath expr="//field[@name='currency_id']" position="after">
                    <field name="invoice_status"/>
                </xpath>

                <!-- Habilitar edición inline en el tree (editar directo en la línea) -->
                <xpath expr="//field[@name='order_line']/tree" position="attributes">
                    <attribute name="editable">bottom</attribute>
                </xpath>

                <!-- Convertir sequence en widget handle para arrastrar y ordenar -->
                <xpath expr="//field[@name='order_line']/tree/field[@name='sequence']" position="attributes">
                    <attribute name="widget">handle</attribute>
                </xpath>

                <!-- Agregar campo # de línea clickeable como primera columna -->
                <xpath expr="//field[@name='order_line']/tree/field[@name='product_id']" position="before">
                    <field name="line_number_display" string="#" readonly="1"/>
                    <button name="action_open_line_form"
                            type="object"
                            icon="fa-edit"
                            class="btn-sm btn-link"
                            title="Editar línea"/>
                </xpath>

                <!-- Insertamos el botón dentro del árbol de las líneas: 2025.02.26 -->
                <xpath expr="//field[@name='order_line']/tree" position="inside">
                    <button name="action_view_line_history"
                            type="object"
                            icon="fa-history"
                            string="Historial" />
                </xpath>

                <!-- Insertar campos después del campo 'effective_date' -->
                <xpath expr="//field[@name='effective_date']" position="after">
                    <field name="account_analytic_id"/>
                    <field name="vehicle_id"/>
                    <field name="payment_term_id"/>
                    <field name="send_all_attachments" />
                </xpath>
                <!-- Agregar campo computado para subtotal-->

                <xpath expr="//field[@name='notes']" position="after">
                    <!-- Sección principal dividida en 2 columnas equilibradas -->
                    <group string="Descuentos y Totales" col="4" colspan="4">
                        
                        <!-- Columna izquierda: Descuentos -->
                        <group string="Configuración de Descuentos" col="4" colspan="4">
                            <field name="global_discount_type"
                                attrs="{'readonly': [('state', 'not in', ['draft', 'sent', 'waiting_for_price_revision'])]}"
                                string="Tipo de Descuento" colspan="4"/>
                            <field name="global_discount_percentage"
                                attrs="{'invisible': [('global_discount_type', 'not in', ['percentage', 'percentage_line'])]}"
                                string="Porcentaje (%)" colspan="1"/>
                            <field name="global_discount_fixed_amount"
                                attrs="{'invisible': [('global_discount_type', 'not in', ['fixed', 'fixed_line'])]}"
                                string="Monto Fijo" colspan="1"/>
                            
                            <!-- Separador visual -->
                            <separator string="Resumen de Descuentos" colspan="2"/>
                            <field name="total_discount_amount_lines"
                                readonly="1"
                                string="Total Descuento (Líneas)" colspan="2"/>
                        </group>
                        
                        
                    </group>
                </xpath>

                <xpath expr="//group[@class='oe_subtotal_footer oe_right']" position="replace">
                    <group col="2">
                        <group string="Totales" col="1">
                            <p/>
                            <field name="subtotal_without_discount" readonly="1" string="Subtotal (Sin Descuentos)"/>
                            <field name="total_discount" readonly="1" string="Total Descuento"/>
                        </group>
                        <group string="Desglose de Impuestos" col="2">

                            <field name="tax_totals"
                                widget="account-tax-totals-field"
                                nolabel="1"
                                readonly="1"/>
                        </group>
                    </group>
                </xpath>

                <xpath expr="//field[@name='order_line']/tree" position="inside">
                    <field name="product_qty" string="Qty" class="text-right"
                        options="{'no_create': True}"
                        widget="float"
                        attrs="{'readonly': [('state', 'not in', ['draft', 'sent', 'waiting_for_price_revision'])]}"
                        decoration-danger="hide == True" />
                </xpath>
                <xpath expr="//field[@name='price_tax']" position="after">
                    <field name="vehicle_id" optional="show" decoration-danger="hide == True"/>
                    <field name="account_analytic_id" optional="show" decoration-danger="hide == True"/>
                    <field name="notes" optional="show" decoration-danger="hide == True"/>
                    <field name="allow_qty_change_line" optional="hide" />
                    <!-- Line Discount Fields -->
                    <field name="discount_type"
                           attrs="{'readonly': [('parent.state', 'not in', ['draft', 'sent', 'waiting_for_price_revision'])]}"
                           optional="show"/>
                    <field name="discount"
                           string="Discount (%)"
                           optional="show"
                           attrs="{
                               'column_invisible': [('discount_type', '!=', 'percentage')],
                               'readonly': [('parent.state', 'not in', ['draft', 'sent', 'waiting_for_price_revision'])],
                               'invisible': [('parent.global_discount_type', 'not in', ['percentage_line'])]
                           }"/>
                    <field name="discount_fixed_amount"
                           string="Desc. Fijo"
                           optional="show"
                           attrs="{
                               'column_invisible': [('discount_type', '=', 'percentage')],
                               'readonly': [('parent.state', 'not in', ['draft', 'sent', 'waiting_for_price_revision'])],

                           }"/>
                    <field name="hide" optional="show" class="text-primary" decoration-danger="hide == True"/>
                    <field name="phase_id" optional="show"/>
                </xpath>
                <xpath expr="//field[@name='product_id']" position="attributes">
                    <attribute name="decoration-danger">hide == True</attribute>
                </xpath>
                <xpath expr="//field[@name='price_unit']" position="attributes">
                    <attribute name="decoration-danger">hide == True</attribute>
                </xpath>
                <xpath expr="//field[@name='taxes_id']" position="attributes">
                    <attribute name="decoration-danger">hide == True</attribute>
                </xpath>
                <xpath expr="//field[@name='price_subtotal']" position="attributes">
                    <attribute name="decoration-danger">hide == True</attribute>
                </xpath>
                <xpath expr="//field[@name='product_uom']" position="attributes">
                    <attribute name="decoration-danger">hide == True</attribute>
                </xpath>

                <!--Agregando historia de productos: 2025.02.26-->
                <xpath expr="//notebook" position="inside">
                    <page string="Historial de Compras">
                        <field name="purchase_history_ids" readonly="1">
                            <tree>
                                <field name="order_id" invisible="1"/>
                                <field name="product_id"/>
                                <field name="product_qty"/>
                                <field name="price_unit"/>
                                <field name="date_planned"/>
                                <field name="partner_id" string="Proveedor" />
                                <!-- Agrega más campos que quieras ver, por ejemplo el estado de la orden -->
                                <field name="state" />
                            </tree>
                            <form>
                                <sheet>
                                    <group>
                                        <field name="product_id"/>
                                        <field name="product_qty"/>
                                        <field name="price_unit"/>
                                        <field name="date_planned"/>
                                    </group>
                                </sheet>
                            </form>
                        </field>
                    </page>
                </xpath>
            </field>
        </record>
        <!-- Vista Form COMPLETA para popup con botón -->
        <record id="view_purchase_order_line_form_complete_popup" model="ir.ui.view">
            <field name="name">purchase.order.line.form.complete.popup</field>
            <field name="model">purchase.order.line</field>
            <field name="priority">1</field>
            <field name="arch" type="xml">
                <form string="Línea de Orden de Compra" create="false" edit="true" delete="true">
                    <header>
                        <button name="unlink"
                                string="🗑️ Eliminar Línea"
                                type="object"
                                class="oe_highlight"
                                confirm="¿Está seguro que desea eliminar esta línea?"
                                attrs="{'invisible': [('state', 'not in', ['draft', 'sent', 'waiting_for_price_revision'])]}"/>
                        <field name="state" widget="statusbar" statusbar_visible="draft,sent,waiting_for_price_revision,purchase,done"/>
                    </header>
                    <sheet>
                        <div class="alert alert-warning" role="alert" attrs="{'invisible': [('hide', '=', False)]}">
                            <strong>⚠️ Línea Oculta:</strong> Esta línea está marcada para ocultarse en reportes
                        </div>

                        <group>
                            <group string="Información del Producto">
                                <field name="order_id" readonly="1"/>
                                <field name="product_id" attrs="{'readonly': [('state', 'not in', ['draft', 'sent', 'waiting_for_price_revision'])]}"/>
                                <field name="name" attrs="{'readonly': [('state', 'not in', ['draft', 'sent', 'waiting_for_price_revision'])]}"/>
                                <field name="product_qty" attrs="{'readonly': [('state', 'not in', ['draft', 'sent', 'waiting_for_price_revision'])]}"/>
                                <field name="product_uom" attrs="{'readonly': [('state', 'not in', ['draft', 'sent', 'waiting_for_price_revision'])]}"/>
                                <field name="date_planned" attrs="{'readonly': [('state', 'not in', ['draft', 'sent', 'waiting_for_price_revision'])]}"/>
                            </group>

                            <group string="Proyecto y Distribución">
                                <field name="account_analytic_id" attrs="{'readonly': [('state', 'not in', ['draft', 'sent', 'waiting_for_price_revision'])]}"/>
                                <field name="analytic_distribution" widget="analytic_distribution" attrs="{'readonly': [('state', 'not in', ['draft', 'sent', 'waiting_for_price_revision'])]}"/>
                                <field name="phase_id" attrs="{'readonly': [('state', 'not in', ['draft', 'sent', 'waiting_for_price_revision'])]}"/>
                                <field name="vehicle_id" attrs="{'readonly': [('state', 'not in', ['draft', 'sent', 'waiting_for_price_revision'])]}"/>
                            </group>
                        </group>

                        <group>
                            <group string="Precios y Descuentos">
                                <field name="price_unit" attrs="{'readonly': [('state', 'not in', ['draft', 'sent', 'waiting_for_price_revision'])]}"/>
                                <field name="discount_type" attrs="{'readonly': [('state', 'not in', ['draft', 'sent', 'waiting_for_price_revision'])]}"/>
                                <field name="discount" attrs="{'invisible': [('discount_type', '!=', 'percentage')], 'readonly': [('state', 'not in', ['draft', 'sent', 'waiting_for_price_revision'])]}"/>
                                <field name="discount_fixed_amount" attrs="{'invisible': [('discount_type', '!=', 'fixed')], 'readonly': [('state', 'not in', ['draft', 'sent', 'waiting_for_price_revision'])]}"/>
                                <field name="taxes_id" widget="many2many_tags" attrs="{'readonly': [('state', 'not in', ['draft', 'sent', 'waiting_for_price_revision'])]}"/>
                            </group>

                            <group string="Opciones">
                                <field name="hide" string="🔴 Ocultar en Reportes" widget="boolean_toggle" attrs="{'readonly': [('state', 'not in', ['draft', 'sent', 'waiting_for_price_revision'])]}"/>
                                <field name="display_type" attrs="{'readonly': [('state', 'not in', ['draft', 'sent', 'waiting_for_price_revision'])]}"/>
                                <field name="sequence" attrs="{'readonly': [('state', 'not in', ['draft', 'sent', 'waiting_for_price_revision'])]}"/>
                            </group>
                        </group>

                        <group>
                            <field name="notes" placeholder="Notas adicionales..." attrs="{'readonly': [('state', 'not in', ['draft', 'sent', 'waiting_for_price_revision'])]}"/>
                        </group>

                        <group string="Totales">
                            <group>
                                <field name="price_subtotal" readonly="1" widget="monetary"/>
                                <field name="price_tax" readonly="1" widget="monetary"/>
                                <field name="price_total" readonly="1" widget="monetary"/>
                            </group>
                            <group>
                                <field name="qty_received" readonly="1"/>
                                <field name="qty_invoiced" readonly="1"/>
                            </group>
                        </group>

                        <!-- Campos invisibles necesarios -->
                        <field name="product_uom_category_id" invisible="1"/>
                        <field name="company_id" invisible="1"/>
                        <field name="currency_id" invisible="1"/>
                        <field name="partner_id" invisible="1"/>
                    </sheet>
                </form>
            </field>
        </record>

        <!-- VISTA COMPLETA ORIGINAL - NO SE USA AHORA -->
        <record id="view_purchase_order_line_form_popup_jc_DISABLED" model="ir.ui.view">
            <field name="name">purchase.order.line.form.popup.jc.disabled</field>
            <field name="model">purchase.order.line</field>
            <field name="active" eval="False"/>
            <field name="arch" type="xml">
                <form string="Línea de Orden de Compra - DISABLED">
                    <header>
                        <field name="state" widget="statusbar" statusbar_visible="draft,sent,waiting_for_price_revision,purchase,done"/>
                        <button name="action_view_line_history" string="Ver Historial Completo" type="object" class="btn-secondary"/>
                    </header>
                    <sheet>
                        <!-- Banner de advertencia para líneas ocultas -->
                        <div class="alert alert-warning" role="alert" attrs="{'invisible': [('hide', '=', False)]}">
                            <strong>⚠️ Línea Oculta:</strong> Esta línea está marcada para ocultarse en reportes
                        </div>

                        <group>
                            <group string="📋 Información General">
                                <field name="order_id" readonly="1" force_save="1"/>
                                <field name="partner_id" readonly="1" string="Proveedor"/>
                                <field name="company_id" invisible="1"/>
                                <field name="currency_id" invisible="1"/>
                                <field name="product_uom_category_id" invisible="1"/>
                                <field name="state" invisible="1"/>
                                <field name="product_id"
                                    attrs="{'readonly': [('state', 'not in', ['draft', 'sent', 'waiting_for_price_revision'])]}"/>
                                <field name="product_categ_id" readonly="1" string="Categoría"/>
                                <field name="name" string="Descripción"
                                    attrs="{'readonly': [('state', 'not in', ['draft', 'sent', 'waiting_for_price_revision'])]}"/>
                                <field name="date_planned" string="Fecha Programada"
                                    attrs="{'readonly': [('state', 'not in', ['draft', 'sent', 'waiting_for_price_revision'])]}"/>
                            </group>

                            <group string="🎯 Proyecto y Distribución">
                                <field name="pr_ref_ids" readonly="1" string="Referencia SdC"/>
                                <field name="account_analytic_id" string="Cuenta Analítica"
                                    attrs="{'readonly': [('state', 'not in', ['draft', 'sent', 'waiting_for_price_revision'])]}"/>
                                <field name="analytic_distribution" widget="analytic_distribution" string="Distribución Analítica"
                                    attrs="{'readonly': [('state', 'not in', ['draft', 'sent', 'waiting_for_price_revision'])]}"/>
                                <field name="phase_id" string="Fase"
                                    attrs="{'readonly': [('state', 'not in', ['draft', 'sent', 'waiting_for_price_revision'])]}"/>
                                <field name="vehicle_id" string="Vehículo"
                                    attrs="{'readonly': [('state', 'not in', ['draft', 'sent', 'waiting_for_price_revision'])]}"/>
                            </group>
                        </group>

                        <group>
                            <group string="📊 Cantidades y Unidades">
                                <field name="product_qty" string="Cantidad Pedida"
                                    attrs="{'readonly': [('state', 'not in', ['draft', 'sent', 'waiting_for_price_revision'])]}"/>
                                <field name="product_uom" string="Unidad de Medida"
                                    attrs="{'readonly': [('state', 'not in', ['draft', 'sent', 'waiting_for_price_revision'])]}"/>
                                <field name="product_packaging_id" string="Empaque" optional="hide"
                                    attrs="{'readonly': [('state', 'not in', ['draft', 'sent', 'waiting_for_price_revision'])]}"/>
                                <separator string="Estado de Recepción y Facturación" colspan="2"/>
                                <field name="qty_received" readonly="1" string="Cantidad Recibida"/>
                                <field name="qty_received_manual" readonly="1" string="Cant. Recibida Manual"/>
                                <field name="qty_invoiced" readonly="1" string="Cantidad Facturada"/>
                                <field name="qty_to_invoice" readonly="1" string="Cantidad por Facturar"/>
                                <field name="x_is_incomplete" readonly="1" string="Recepción Incompleta"/>
                            </group>

                            <group string="💰 Precios y Descuentos">
                                <field name="price_unit" string="Precio Unitario"
                                    attrs="{'readonly': [('state', 'not in', ['draft', 'sent', 'waiting_for_price_revision'])]}"/>
                                <field name="discount_type" string="Tipo de Descuento"
                                    attrs="{'readonly': [('state', 'not in', ['draft', 'sent', 'waiting_for_price_revision'])]}"/>
                                <field name="discount"
                                    string="Descuento (%)"
                                    attrs="{
                                        'invisible': [('discount_type', '!=', 'percentage')],
                                        'readonly': [('state', 'not in', ['draft', 'sent', 'waiting_for_price_revision'])]
                                    }"/>
                                <field name="discount_fixed_amount"
                                    string="Descuento Fijo"
                                    attrs="{
                                        'invisible': [('discount_type', '!=', 'fixed')],
                                        'readonly': [('state', 'not in', ['draft', 'sent', 'waiting_for_price_revision'])]
                                    }"/>
                                <field name="price_unit_discounted" readonly="1" string="Precio Unitario con Desc."/>
                            </group>
                        </group>

                        <group>
                            <group string="💵 Impuestos y Totales">
                                <field name="taxes_id" widget="many2many_tags" string="Impuestos"
                                    attrs="{'readonly': [('state', 'not in', ['draft', 'sent', 'waiting_for_price_revision'])]}"/>
                                <separator string="Totales Calculados" colspan="2"/>
                                <field name="price_subtotal" readonly="1" widget="monetary" string="Subtotal"/>
                                <field name="price_tax" readonly="1" widget="monetary" string="Impuesto"/>
                                <field name="price_total" readonly="1" widget="monetary" string="Total"/>
                                <separator string="Totales Visibles (Sin Ocultas)" colspan="2"/>
                                <field name="price_subtotal_visible" readonly="1" widget="monetary" string="Subtotal Visible"/>
                                <field name="price_tax_visible" readonly="1" widget="monetary" string="Impuesto Visible"/>
                                <field name="price_total_visible" readonly="1" widget="monetary" string="Total Visible"/>
                            </group>

                            <group string="⚙️ Opciones y Configuración">
                                <field name="hide" string="🔴 Ocultar en Reportes" widget="boolean_toggle"
                                    attrs="{'readonly': [('state', 'not in', ['draft', 'sent', 'waiting_for_price_revision'])]}"/>
                                <field name="allow_qty_change_line" readonly="1" string="Permitir Cambio Cant."/>
                                <field name="display_type" string="Tipo de Visualización"
                                    attrs="{'readonly': [('state', 'not in', ['draft', 'sent', 'waiting_for_price_revision'])]}"/>
                                <field name="sequence" string="Secuencia/Orden"
                                    attrs="{'readonly': [('state', 'not in', ['draft', 'sent', 'waiting_for_price_revision'])]}"/>
                                <field name="product_type" readonly="1" string="Tipo de Producto"/>
                                <separator string="Notas Adicionales" colspan="2"/>
                                <field name="notes" nolabel="1" placeholder="Notas adicionales para esta línea..."
                                    attrs="{'readonly': [('state', 'not in', ['draft', 'sent', 'waiting_for_price_revision'])]}"/>
                            </group>
                        </group>

                        <!-- Sección adicional: Movimientos de Stock -->
                        <group string="📦 Movimientos de Stock" attrs="{'invisible': [('product_type', '=', 'service')]}">
                            <field name="move_ids" readonly="1" nolabel="1">
                                <tree>
                                    <field name="reference"/>
                                    <field name="picking_id"/>
                                    <field name="product_uom_qty" string="Cantidad"/>
                                    <field name="quantity_done" string="Realizado"/>
                                    <field name="state"/>
                                    <field name="date" string="Fecha"/>
                                </tree>
                            </field>
                        </group>

                        <notebook>
                            <page string="📚 Historial de Compras">
                                <group>
                                    <group>
                                        <field name="pr_request_date" readonly="1" string="Fecha SdC"/>
                                        <field name="date_order" readonly="1" string="Fecha Orden"/>
                                    </group>
                                    <group>
                                        <field name="days_deferred" readonly="1" string="Días Diferidos"/>
                                        <field name="hours_deferred" readonly="1" string="Horas Diferidas"/>
                                    </group>
                                </group>
                                <separator string="Compras Anteriores del Mismo Producto"/>
                                <field name="line_history_ids" readonly="1" nolabel="1">
                                    <tree>
                                        <field name="order_id" string="Orden"/>
                                        <field name="partner_id" string="Proveedor"/>
                                        <field name="date_order" string="Fecha"/>
                                        <field name="product_qty" string="Cantidad"/>
                                        <field name="price_unit" string="P.U."/>
                                        <field name="discount" string="Desc.%"/>
                                        <field name="price_total" string="Total"/>
                                        <field name="state" string="Estado"/>
                                    </tree>
                                </field>
                            </page>

                            <page string="📄 Facturación" attrs="{'invisible': [('invoice_lines', '=', [])]}">
                                <field name="invoice_lines" readonly="1" nolabel="1">
                                    <tree>
                                        <field name="move_id" string="Factura"/>
                                        <field name="date" string="Fecha"/>
                                        <field name="quantity" string="Cantidad"/>
                                        <field name="price_unit" string="P.U."/>
                                        <field name="price_subtotal" string="Subtotal"/>
                                    </tree>
                                </field>
                            </page>

                            <page string="🔍 Información Técnica">
                                <group>
                                    <group string="IDs y Referencias">
                                        <field name="id" readonly="1"/>
                                        <field name="order_id" readonly="1"/>
                                        <field name="company_id" readonly="1"/>
                                        <field name="currency_id" readonly="1"/>
                                        <field name="partner_id" readonly="1"/>
                                    </group>
                                    <group string="Fechas">
                                        <field name="create_date" readonly="1" string="Fecha Creación"/>
                                        <field name="write_date" readonly="1" string="Última Modificación"/>
                                        <field name="create_uid" readonly="1" string="Creado por"/>
                                        <field name="write_uid" readonly="1" string="Modificado por"/>
                                        <field name="date_order" readonly="1" string="Fecha Orden"/>
                                    </group>
                                </group>
                                <group>
                                    <group string="Campos Calculados">
                                        <field name="price_unit_discounted" readonly="1" string="P.U. con Descuento"/>
                                        <field name="x_is_incomplete" readonly="1" string="Recepción Incompleta"/>
                                    </group>
                                    <group string="Configuración">
                                        <field name="product_uom_category_id" readonly="1" string="Categoría UdM"/>
                                    </group>
                                </group>
                            </page>

                            <page string="📊 Resumen Completo">
                                <group col="4">
                                    <group string="Producto" colspan="2">
                                        <field name="product_id" readonly="1"/>
                                        <field name="product_categ_id" readonly="1"/>
                                        <field name="product_type" readonly="1"/>
                                        <field name="name" readonly="1"/>
                                    </group>
                                    <group string="Cantidades" colspan="2">
                                        <label for="product_qty" string="Pedida / Recibida / Facturada"/>
                                        <div>
                                            <field name="product_qty" readonly="1" class="oe_inline"/> /
                                            <field name="qty_received" readonly="1" class="oe_inline"/> /
                                            <field name="qty_invoiced" readonly="1" class="oe_inline"/>
                                        </div>
                                        <field name="qty_to_invoice" readonly="1"/>
                                        <field name="product_uom" readonly="1"/>
                                    </group>
                                    <group string="Precios" colspan="2">
                                        <field name="price_unit" readonly="1"/>
                                        <field name="discount" readonly="1"/>
                                        <field name="discount_type" readonly="1"/>
                                        <field name="discount_fixed_amount" readonly="1"/>
                                        <field name="price_unit_discounted" readonly="1"/>
                                    </group>
                                    <group string="Totales" colspan="2">
                                        <field name="price_subtotal" readonly="1" widget="monetary"/>
                                        <field name="price_tax" readonly="1" widget="monetary"/>
                                        <field name="price_total" readonly="1" widget="monetary"/>
                                    </group>
                                    <group string="Proyecto" colspan="2">
                                        <field name="account_analytic_id" readonly="1"/>
                                        <field name="phase_id" readonly="1"/>
                                        <field name="vehicle_id" readonly="1"/>
                                    </group>
                                    <group string="Estado" colspan="2">
                                        <field name="state" readonly="1"/>
                                        <field name="invoice_status" readonly="1"/>
                                        <field name="hide" readonly="1"/>
                                        <field name="display_type" readonly="1"/>
                                    </group>
                                </group>
                            </page>
                        </notebook>
                    </sheet>
                </form>
            </field>
        </record>

        <!-- views/purchase_order_line_history.xml -->
        <record id="view_purchase_order_line_form_inherit_history" model="ir.ui.view">
            <field name="name">purchase.order.line.form.inherit.history</field>
            <field name="model">purchase.order.line</field>
            <field name="inherit_id" ref="purchase.purchase_order_line_form2"/>
            <!-- Ajusta el ref al XML que define el formulario de la línea -->
            <field name="arch" type="xml">
                <xpath expr="//field[@name='price_unit']" position="after">
                    <field name="discount_type" attrs="{'readonly': [('parent.state', 'not in', ['draft', 'sent', 'waiting_for_price_revision'])]}"/>
                    <field name="discount" string="Discount (%)" attrs="{'invisible': [('discount_type', '!=', 'percentage')], 'readonly': [('parent.state', 'not in', ['draft', 'sent', 'waiting_for_price_revision'])]}"/>
                    <field name="discount_fixed_amount" string="Discount Amount" attrs="{'invisible': [('discount_type', '!=', 'fixed')], 'readonly': [('parent.state', 'not in', ['draft', 'sent', 'waiting_for_price_revision'])]}"/>
                </xpath>
                <!-- Donde quieras insertarlo; por ejemplo dentro de <sheet> -->
                <xpath expr="//sheet" position="inside">
                    <notebook>
                        <page string="Historial de esta línea">
                            <field name="line_history_ids" readonly="1">
                                <tree>
                                    <field name="order_id"/>
                                    <field name="product_id"/>
                                    <field name="product_qty"/>
                                    <field name="price_unit"/>
                                    <field name="date_planned"/>
                                    <field name="state"/>
                                </tree>
                                <form>
                                    <sheet>
                                        <group>
                                            <field name="order_id"/>
                                            <field name="product_id"/>
                                            <field name="product_qty"/>
                                            <field name="price_unit"/>
                                            <field name="date_planned"/>
                                            <field name="state"/>
                                        </group>
                                    </sheet>
                                </form>
                            </field>
                        </page>
                    </notebook>
                </xpath>
            </field>
        </record>
        <!-- Heredar la vista con ID externo 'purchase.purchase_history_tree': 2025.02.26 -->
        <record id="purchase_purchase_history_tree_inherit" model="ir.ui.view">
            <field name="name">purchase.history.tree.inherit</field>
            <field name="model">purchase.order.line</field>
            <!-- Reemplaza con el ID exacto de la vista que quieres heredar -->
            <field name="inherit_id" ref="purchase.purchase_history_tree"/>
            <field name="arch" type="xml">
                <!-- Insertar campos dentro del tree existente -->
                <xpath expr="//tree" position="inside">
                    <!-- Fecha de la OC (si el modelo base es purchase.order.line y la fecha vive en order_id) -->
                    <field name="date_order" string="Fecha OC"/>

                    <!-- Estado de la Orden (purchase, done, cancel, etc.) -->
                    <field name="state" string="Estatus"/>

                    <!-- Campo pr_ref_ids (ajusta el string si deseas) -->
                    <field name="pr_ref_ids" string="PR Ref IDs" widget="Many2one"/>
                </xpath>
            </field>
        </record>
        <!-- Heredar la vista form de purchase_discount.purchase.order.form -->
        <record id="purchase_order_form_discounted_price" model="ir.ui.view">
            <field name="name">purchase.order.form.discounted.price</field>
            <field name="model">purchase.order</field>
            <field name="inherit_id" ref="purchase_discount.purchase_order_form"/>
            <field name="arch" type="xml">
                <!-- Insertar campo después del campo 'price_unit' -->
                <xpath expr="//field[@name='price_unit']" position="after">
                    <field name="price_unit_discounted" string="P.U.Desc."
                        decoration-danger="hide == True" readonly="1"/>
                </xpath>
            </field>
        </record>
        <!-- Copyright 2025 - jobcostphasecat
        Heredar la vista form de purchase.order.form del módulo purchase_order_general_discount
        License AGPL-3.0 or later (https://www.gnu.org/licenses/agpl). -->
        <record id="purchase_order_form_inherit_jobcostphasecat" model="ir.ui.view">
            <field name="model">purchase.order</field>
            <field name="inherit_id" ref="purchase_order_general_discount.purchase_order_form" />
            <field name="arch" type="xml">
            <!-- Protegemos el botón y el campo general_discount 
            <xpath expr="//button[@name='action_update_general_discount']" position="attributes">
                <attribute name="attrs">{'invisible': ['|', ('state', 'not in', ['draft', 'sent', 'waiting_for_price_revision']), ('global_discount_type', '!=', False)]}</attribute>
            </xpath>
            <xpath expr="//field[@name='general_discount']" position="attributes">
                <attribute name="attrs">{'readonly': [('state', 'not in', ['draft', 'sent', 'waiting_for_price_revision'])], 'invisible': [('global_discount_type', '!=', False)]}</attribute>
            </xpath>
            -->
            <!--Ocultar el campo general_discount-->
            <xpath expr="//field[@name='general_discount']" position="attributes">
                <attribute name="invisible">1</attribute>
            </xpath>
            <!--Ocultar boton: action_update_general_discount-->
            <xpath expr="//button[@name='action_update_general_discount']" position="attributes">
                <attribute name="invisible">1</attribute>
            </xpath>

            </field>
        </record>
        <!-- Heredar la vista form de purchase.order.form -->
        <record id="purchase_order_form_approval_rules_jc" model="ir.ui.view">
            <field name="name">purchase.order.form.approval.rules.jc</field>
            <field name="model">purchase.order</field>
            <field name="inherit_id" ref="purchase.purchase_order_form"/>
            <field name="arch" type="xml">
                <!-- Cambiar los atributos del boton: action_rfq_send
                    <attribute name='attrs'>{'invisible': ['|', ('state','!=','draft'), ('purchase_order_approval_rule_ids', '!=', [])]} -->
                <xpath expr="//button[@name='action_rfq_send']" position="replace">
                    <button name="action_rfq_send"
                        states="draft"
                        string="1.Enviar SdP e-mail"
                        type="object" context="{'send_rfq':True}"
                        class="oe_highlight" data-hotkey="g"
                        groups="jobcostphasecat.group_rfq_user"/>
                    <button name="action_rfq_send_jc"
                        states="purchase"
                        string="7.Bloquear - Enviado P.O. e-mail"
                        type="object" context="{'send_rfq':False}"
                        class="btn-primary oe_highlight" data-hotkey="g"
                        groups="jobcostphasecat.group_rfq_sender_po"/>
                    <button name="action_back"
                        string="Regresar"
                        type="object"
                        class="btn-secondary"
                        groups="jobcostphasecat.group_rfq_user"
                        attrs="{'invisible': [('state', 'in', ['done','purchase','draft'])]}"/>
                </xpath>

                <!-- Eliminar el botón de confirmación -->
                <xpath expr="//button[@name='button_confirm']" position="attributes">
                    <attribute name="attrs">{'invisible': [('state', '!=', 'done-plus')]}</attribute>
                </xpath>
                <!-- Insertar boton: Envio para Revisión de Precio-->
                <xpath expr="//button[@name='button_confirm']" position="before">
                    <button name="action_waiting_for_price_revision"
                        string="2.Envío a Revisión de Precios" type="object"
                        class="btn-primary oe_highlight"
                        groups="jobcostphasecat.group_rfq_updater"
                        attrs="{'invisible': [('state', '!=', 'sent') ] }"
                    />
                    <button name="action_discard"
                        string="Descartar"
                        type="object"
                        class="btn-secondary"
                        groups="jobcostphasecat.group_rfq_approver"
                        attrs="{'invisible': [('state', '!=', 'waiting_for_price_revision')]}"
                    />
                </xpath>
                <!-- Insertar boton: Envio para Aprobación de Precio-->
                <xpath expr="//button[@name='button_confirm']" position="before">
                    <button name="action_waiting_for_price_approval"
                        string="3.Envío a Aprobación de Precios" type="object"
                        class="btn-primary oe_highlight"
                        groups="jobcostphasecat.group_rfq_reviewer"
                        attrs="{'invisible': [('state', '!=', 'waiting_for_price_revision') ] }"
                    />
                    <button name="reject_waiting_for_price_revision"
                        string="Rechazo en Revisión de Precios" type="object"
                        groups="jobcostphasecat.group_rfq_reviewer"
                        attrs="{'invisible': [('state', '!=', 'waiting_for_price_revision') ] }"
                    />
                </xpath>
                <!-- Insertar boton: Envio para Aprobación de SdP -->
                <xpath expr="//button[@name='button_confirm']" position="before">
                    <button name="action_waiting_for_approval"
                        string="4.Envío a Aprobación de SdP" type="object"
                        class="btn-primary oe_highlight"
                        groups="jobcostphasecat.group_rfq_price_approver"
                        attrs="{'invisible': [('state', '!=', 'waiting_for_price_approval') ] }"
                    />
                    <button name="reject_waiting_for_price_approval"
                        string="Rechazo en Aprobación de Precios" type="object"
                        class="oe_buttom"
                        groups="jobcostphasecat.group_rfq_price_approver"
                        attrs="{'invisible': [('state', '!=', 'waiting_for_price_approval') ] }"
                    />
                </xpath>

                <!-- Insertar boton: Rechazar Aprobación de SdP y Envio a Aprobacion de Compras
                        Este botón debe estar solo lectura hasta que se haya aprobado todas
                        las reglas de aprobacion-->
                <xpath expr="//button[@name='button_confirm']" position="before">
                    <button name="action_send_to_approve_purchase"
                        string="5.Envío a Aprob.@Compras" type="object"
                        class="btn-primary oe_highlight"
                        groups="jobcostphasecat.group_rfq_approver"
                        attrs="{'invisible': [('state', '!=', 'waiting_for_approval')]}"
                    />
                    <button name="reject_waiting_for_approval"
                        string="Rechazo de Aprobación de SdP" type="object"
                        groups="jobcostphasecat.group_rfq_approver"
                        attrs="{'invisible': [('state', '!=', 'waiting_for_approval') ] }"
                    />
                </xpath>
                <!-- Cambio de atributos del botón: action_button_approve. 
                <xpath expr="//button[@name='button_confirm']" position="before">
                    <button name="action_buttom_approve_jc"
                        string="Aprobar SdP" type="object"
                        groups="jobcostphasecat.group_rfq_approver"
                        attrs="{'invisible': [('state', '!=', 'waiting_for_approval') ] }"
                    />
                </xpath>
                -->
                <!-- Botón Enviar P.O.-->
                <xpath expr="//button[@name='action_rfq_send']" position="before">
                </xpath>
                <!-- Cambiar los atributos del boton: button_approve -->
                <xpath expr="//button[@name='button_approve']" position="attributes">
                    <attribute name="string">6.Confirmar</attribute>
                    <attribute name='attrs'>{'invisible': [('state','!=','to approve') ]} </attribute>
                    <attribute name="groups">jobcostphasecat.group_rfq_confirn</attribute>
                </xpath>
            </field>
        </record>
        <!-- Heredar las busquedas de purchase.order-->
        <record id="purchase_order_pr_ref_id_filter" model="ir.ui.view">
            <field name="name">purchase.order.pr.ref.id.filter</field>
            <field name="model">purchase.order</field>
            <field name="inherit_id" ref="purchase.view_purchase_order_filter"/>
            <field name="arch" type="xml">
                <xpath expr="//search" position="inside">
                    <!--
                    <filter string="PR Reference ID" name="filter_pr_ref_id" domain="[('pr_ref_id', 'ilike', self)]"/>
                    -->
                    <field name="pr_ref_id" />
                </xpath>
            </field>
        </record>
    </data>
</odoo>
